jQuery(document).ready(function() {
    // Oskari.app.loadAppSetup(ajaxUrl + 'action_route=GetAppSetup', window.controlParams, function() {
    //     console.log(this);
    //     jQuery('#mapdiv').append('Unable to start');
    // });
    Oskari.app.loadES6AppSetup();
});